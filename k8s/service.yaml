apiVersion: v1

kind: Service

metadata:
  name: cloud-cost-dashboard

spec:

  selector:
    app: cloud-cost-dashboard

  type: NodePort

  ports:

  - port: 80

    targetPort: 8080

    nodePort: 30080


